<ul class="sg-colors">
	{% for key, color in colors %}
		<li>
			<span class="sg-swatch" style="background: {{ color.hex }};"></span>
			<span class="sg-label">{{ color.name }}: {{ color.hex }}</span>
		</li>
	{% endfor %}
</ul>
