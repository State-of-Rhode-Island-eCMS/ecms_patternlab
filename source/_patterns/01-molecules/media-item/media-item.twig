{% set classes = "" %}
{% set classes = classes ~ "media-" ~ type %}
{% set classes = classes ~ " " ~ size %}
{% set classes = classes ~ " " ~ align %}

<figure class="media-item {{ classes }}">
  {% if type == 'img' %}
    <img src="{{ url }}" alt="{{ alt }}" />
  {% elseif type == 'video' %}
    <div class="media-item__video">{{ source }}</div>
  {% endif %}
  {% if caption %}
    <figcaption>{{ caption }}</figcaption>
  {% endif %}
</figure>